/** IEC 60715 modular width: one TE (horizontal pitch) = 17.5 mm */
export const MM_PER_TE = 17.5

/** Typical single-row DIN module height on rail (drawing vertical pitch). */
export const MM_PER_DIN_ROW = 90

/** Shown in UI tooltips — documents the scale convention. */
export const DIN_SCALE_TOOLTIP =
  'Scale: 17.5 mm per TE horizontally (IEC 60715 modular width). 90 mm per row vertically (typical DIN module height). Cell sizes on screen are proportional to real millimetres. IEC modular gear (including common KNX DIN modules) uses the same 17.5 mm horizontal pitch per module (1 TE).'
